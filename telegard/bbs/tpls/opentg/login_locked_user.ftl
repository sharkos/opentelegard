${color.blink}${color.brightred}!! ACCOUNT LOCKED !!${color.reset}
${color.white}
This user account has been locked either for exceeding
the set number of failed login attempts, or suspended
by the system operator for some other reason. To clear
the account lock, you should request a password reset
by either using the self-service portal or contacting
the system operator by email.
${color.reset}