
${color.brightcyan}<#if amount?has_content>${amount?string}</#if> ${color.cyan}minutes deducted from this session.

Session Time Remaining: ${color.brightyellow}<#if remain?has_content>${remain?string}</#if>
${color.norm}