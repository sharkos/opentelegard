
${color.brightcyan}<#if amount?has_content>${amount?string}</#if> ${color.cyan}minutes deducted from your balance.

Session Time Remaining: ${color.brightyellow}<#if remain?has_content>${remain?string}</#if>
${color.norm}